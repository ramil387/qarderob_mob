import { View, Text } from 'react-native';
import React from 'react';
import { observer } from 'mobx-react-lite';

const CategoryFilterPage = () => {
    return (
        <View>
            <Text>CategoryFilterPage</Text>
        </View>
    );
};

export default observer(CategoryFilterPage);
